<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.github.jpp</groupId>
    <artifactId>examples</artifactId>
    <version>0.1.0</version>
    <relativePath>../pom.xml</relativePath>
  </parent>

  <artifactId>customer-example</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>JPP Customer Example</name>

  <properties>
    <maven.compiler.release>11</maven.compiler.release>
  </properties>

  <build>
    <plugins>
      <plugin>
        <groupId>io.github.jpp</groupId>
        <artifactId>jpp-maven-plugin</artifactId>
        <version>${project.parent.version}</version>
        <configuration>
          <inputDirectory>${project.basedir}/src/main/jpp</inputDirectory>
          <outputDirectory>${project.build.directory}/generated-sources/jpp</outputDirectory>
        </configuration>
        <executions>
          <execution>
            <id>generate-jpp-sources</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>generate</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
